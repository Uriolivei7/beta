version = 1

cloudstream {
    language = "es"
    description = "Series clásicas y dibujos retro en español latino"
    authors = listOf("Ranita")

    status = 1
    tvTypes = listOf(
        "TvSeries",
        "Cartoon",
    )
}
