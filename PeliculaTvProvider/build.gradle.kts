version = 1

cloudstream {
    language = "mx"
    description = "Películas y Series vía TMDB + embeds públicos"
    authors = listOf("Ranita")

    status = 1
    tvTypes = listOf("Movie", "TvSeries")

    iconUrl = "https://peliculontv.netlify.app/favicon.ico"
}
